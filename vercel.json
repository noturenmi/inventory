{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/swagger/(.*)", "dest": "/public/swagger/$1" },
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}